import {ImageInfo} from "./ImageInfo";

export class People {
    name: string;
    photo?: ImageInfo;

    // constructor(name: string, photo: ImageInfo = null) {
    //     this.name = name;
    //     this.photo = photo;
    // }
}